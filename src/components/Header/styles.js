import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container:{
        alignItems: 'center',
        paddingHorizontal: 0,
        paddingTop: 40,
        paddingBottom: 30,
    },

    top:{
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 5,
    },

    title:{
        fontSize: 18,
        color: '#fff',
        fontWeight: 'bold',
        marginLeft: 8
    }
});

export default styles;